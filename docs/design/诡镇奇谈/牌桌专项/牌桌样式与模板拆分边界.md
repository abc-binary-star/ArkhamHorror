# 牌桌样式与模板拆分边界

基准提交：`b92c00398a`。文中行号均指该提交下的 `frontend/src/arkham/components/Scenario.vue`。

本文是**两个 agent 的分工约定**，不是设计方案。目的是让「结构瘦身」与「视觉/配色」两类改动不互相返工。改动前先读本文件，改完若边界变化请更新它。

## 1. 现状

| 段 | 行范围 | 行数 |
| --- | --- | --- |
| `<script setup>` | 1–2202 | 2,202 |
| `<template>` | 2204–3427 | 1,224 |
| `<style scoped>` | 3429–6986 | 3,558 |

样式块共 415 条叶子规则，分为两类：

| 类别 | 规则数 | 行数 | 占比 |
| --- | --- | --- | --- |
| 含 `:deep()`（跨组件版式） | 124 | 782 | 22% |
| 不含 `:deep()`（Scenario 自身标记的样式） | 291 | 2,297 | 65% |

含 `:deep()` 的 782 行分布：`@media (min-width: 1200px)` 内 649 行、顶层 123 行、`prefers-reduced-motion` 内 10 行。

` :deep()` 目标集中在 Player / PlayerTabs 内部，且绝大多数被 `.scenario-body--multiseat > #player-zone` 约束——也就是**多座位桌面牌桌这一种状态下的版式**：

| 目标类 | 次数 | 宿主 |
| --- | --- | --- |
| `ul.tabs__header` | 13 | PlayerTabs |
| `.hand-area` | 11 | Player |
| `.player-cards` | 10 | Player |
| `.in-play-row` | 9 | Player |
| `.act-row` / `.act-container` | 6 / 6 | ScenarioDeck / Act |
| `.agenda-card` | 5 | Agenda |
| `.asset-zone` / `.equip-slots` / `.discard-empty` / `.investigator-controls` | 各 4 | Player |

非 ` :deep()` 的 2,297 行按选择器首段归族，前几大族：

| 行数 | 选择器首段 | 归属区块 |
| --- | --- | --- |
| 250 | `.scenario-body--multiseat` | 桌面牌桌骨架 |
| 181 | `.scenario-body` | 桌面牌桌骨架 |
| 71 | `.location-cards-container` | 地图 |
| 62 | `#player-zone` | 玩家区 |
| 59 | `.tri-button` | 剧情档案栏 |
| 58 | `.scenario-guide-card-wrapper` | 剧情档案栏 |
| 40 | `.scenario-cards` | 剧情/遭遇区 |
| 36 | `.buttons-row` | 通用 |
| 35 | `.teammate-detail__identity` | 队友栏 |
| 35 | `.workbench-label` | 工作台 |
| 30 | `.location-cards-scroller` | 地图 |
| 29 | `.zoom-btn` | 地图工具 |
| 28 | `.map-corner-controls` | 地图工具 |

**结论：跨组件版式占 22%，其余 65% 是 Scenario 自身标记的样式，可以随 template 分区一起搬。** 因此抽取的先后顺序应「先搬自己那 65%，最后处理跨组件那 22%」。

## 2. 编译后的作用域语义（决定执行配方，已从构建产物核实）

```
源码  :deep(.card-frame)          →  产物  [data-v-069dd406] .card-frame
源码  .scenario-progress          →  产物  .scenario-progress[data-v-069dd406]
```

推论：

1. **`:deep()` 规则能扛住 template 抽取**：它只要求「祖先带本组件 scope id」，不要求目标元素自己带。子组件的根节点继承父组件 scope id，所以子组件内部的后代仍被命中。
2. **普通规则扛不住**：元素一旦搬进子组件模板，就不再带父组件 scope id，`.foo[data-v-父]` 立即失效。
3. 把普通规则改写成 `:deep()` 后，specificity 不变：`.foo[data-v-A]` 与 `[data-v-A] .foo` 都是 (0,2,0)。

## 3. 归属边界

| 区域 | 归属 | 依据 |
| --- | --- | --- |
| `Scenario.vue` 的 template，以及 2,297 行自持样式 | 结构瘦身（本 agent） | 边界自洽 |
| 782 行 `:deep()` 跨组件版式 | **视觉 agent**，与其 Player / Investigator / PlayerTabs 片段化一起搬 | 见 §4 前置条件 |
| `Player.vue` / `Investigator.vue` / `PlayerTabs.vue` 的模板与样式、配色 | 视觉 agent | 其《游玩界面整体重构方案》第 3 条 + 配色归其所有 |
| `styles/*.css`、`locales/**` | 视觉 agent | 同上 |

## 4. 前置条件与顺序

782 行 `:deep()` 版式**必须**排在 Player / Investigator / PlayerTabs 片段化之后：那些规则靠 `:deep()` 从外部给这些组件的内部定位，且条件挂在 Scenario 的 DOM 状态上（`.scenario-body--multiseat`、`#player-zone`）。组件结构一变，规则的落点、条件来源都要跟着改，提前搬必然搬两遍。

推进顺序：

1. ✅ **配方验证**——`ScenarioPhases.vue`（阶段轨道）已完成，specificity 逐条核对无变化。
2. 继续抽自持区块，连带其自身样式：地图工具（`.map-corner-controls`、`.zoom-btn`、`.location-cards-scroller`）、剧情档案栏（`.scenario-guide-card-wrapper`、`.tri-button`、`.teammate-*`）、工作台标签（`.workbench-label`）。
3. 视觉 agent 完成 Player / Investigator / PlayerTabs 片段化。
4. 迁移 782 行 `:deep()` 版式到各自组件。

## 5. 执行配方

抽一段 template 到子组件时：

1. 该段关联的**普通规则**：搬进子组件的 `<style scoped>`。选择器里的父组件前缀（如 `.scenario > .phases`）无法在子组件里表达，改写为子组件自己的根类（`.phases`）。specificity 不变。
2. 该段关联的**普通规则**若不搬（例如被父组件的布局条件约束）：改写成 `:deep()` 留在原处。specificity 不变。
3. 该段若被 `:deep()` 规则命中：**不动**。`:deep()` 规则在抽取后自动继续生效。
4. 媒体查询：随规则一起搬。搬后注意子组件的 CSS 与父组件 CSS 落在不同 chunk，**同权重规则的先后顺序可能改变**——这是唯一需要目视确认的点。

## 6. 验收要求

本仓库《游玩界面整体重构方案》§9 已写明「布局效果必须以真实浏览器截图和操作为证，静态检查不能替代」。结构瘦身无法自行完成这条，因此：

- 每个 slice 交付后由用户在真实浏览器回读对应区域并截图确认；
- 确认前不进入下一个 slice；
- 类型检查、构建、格式检查、已有测试是必要条件，不是充分条件。

## 7. 已知债务

- `Scenario.vue` 样式块中的无引用规则：`.scenario-progress`、`.scenario-progress__item`、`.scenario-progress__item span`、`.scenario-progress__item strong`、`.scenario-progress p`（行 6326–6342），全仓库无对应标记，可直接删。
- 782 行 `:deep()` 跨组件版式：见 §4。

## 8. 勘误

本文件在 `b92c00398a` 之前的版本写着「37 条 `:deep()` 规则、2,260 行、占 60%」。那是统计脚本的错：花括号配对没有排除跨行注释里的 `{}`，把整段 `@media` 容器误当成一条 `:deep()` 规则并重复计数。正确数字见 §1（124 条 / 782 行 / 22%），结论方向未变但量级不同，故顺序改为「先搬自持的 65%」。
